using CharactersStats;
using Prefab;
using System;
using UnityEngine;

public class CharacterModel : CharacterModelBase
{

    public CharacterModel(int id, Enum type, Stats stats)
        : base(id, type, stats)
    {
    }
}
